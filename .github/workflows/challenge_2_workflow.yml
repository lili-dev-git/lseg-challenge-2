name: challnege_2_workflow

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run zero-dependency tests
        run: python path_lookup.py
